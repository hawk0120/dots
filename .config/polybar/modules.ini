# Optional: enable shadows if you like them
shadow = true;
shadow-radius = 10;
shadow-offset-x = -5;
shadow-offset-y = -5;

[module/systray]
type = internal/tray
format-margin = 8pt
tray-spacing = 16pt

[module/battery]
type = internal/battery
full-at = 99
low-at = 5
battery = BAT0
adapter = AC


[module/xworkspaces]
type = internal/xworkspaces

label-active = %name%
label-active-background = ${colors.background-alt}
label-active-underline= ${colors.primary}
label-active-padding = 1

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${colors.disabled}
label-empty-padding = 1

[module/xwindow]
type = internal/xwindow
label = %title:0:60:...%

[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /

label-mounted = %{F#F0C674}%mountpoint%%{F-} %percentage_used%%

label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}

[module/pulseaudio]
type = internal/pulseaudio

format-volume-prefix = "VOL "
format-volume-prefix-foreground = ${colors.primary}
format-volume = <label-volume>

label-volume = %percentage%%

label-muted = muted
label-muted-foreground = ${colors.disabled}


[module/memory]
type = internal/memory
interval = 2
format-prefix = "RAM "
format-background = ${colors.background}
format-foreground = ${colors.foreground}
label = %percentage_used:2%%

[module/cpu]
type = internal/cpu
interval = 2
format-prefix = "CPU "
format-background = ${colors.background}
format-foreground = ${colors.foreground}
label = %percentage:2%%

[module/date]
type = internal/date
interval = 5

date = %d-%m %H:%M
label = %date%
label-foreground = ${colors.cyan}

click-left = /home/hawk0120/.local/bin/polybar_calendar

[module/pomodoro]
type = custom/script
exec = ~/.local/bin/polybar_bash_wrapper.sh
interval = 1
click-left = ~/.local/bin/polybar_bash_wrapper.sh  toggle
click-right = ~/.local/bin/polybar_bash_wrapper.sh reset

[settings]
screenchange-reload = true
pseudo-transparency = true

; vim:ft=dosini

